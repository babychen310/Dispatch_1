# Python 3.10
# TensorFlow 2.15.0 backend (as specified in the paper, Appendix B)

tensorflow==2.15.0
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1        # for reading .xlsx data files
jupyter>=1.0
jupyterlab>=4.0

# --- Dispatch optimization solver ---
# Uncomment / install the solver actually used in your experiments:
# gurobipy>=9.5        # if Gurobi
# cplex>=12.10          # if IBM CPLEX
# pulp>=2.7              # if PuLP (with CBC)
# cvxpy>=1.4             # if CVXPY
# (scipy is already listed above if scipy.optimize was used)
